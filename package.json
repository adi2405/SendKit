{
  "name": "sendkit-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "lint": "oxlint . --deny-warnings",
    "lint:fix": "oxlint . --fix",
    "typecheck": "tsc --noEmit",
    "build:core": "bun run --filter @dev_aditya_joshi/sendkit-core build",
    "build:cli": "bun run --filter @dev_aditya_joshi/sendkit build",
    "build:local-mcp": "bun run --filter @dev_aditya_joshi/sendkit-mcp build",
    "dev:cli": "bun run packages/cli/src/index.ts",
    "dev:local-mcp": "bun run packages/local-mcp/src/index.ts",
    "dev:remote-mcp": "bun run apps/remote-mcp/src/index.ts",
    "release:pack:core": "bun run --filter @dev_aditya_joshi/sendkit-core pack:dry",
    "release:pack:cli": "bun run --filter @dev_aditya_joshi/sendkit pack:dry",
    "release:pack:local-mcp": "bun run --filter @dev_aditya_joshi/sendkit-mcp pack:dry"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  }
}
